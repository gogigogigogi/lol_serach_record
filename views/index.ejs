<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%- include("./include/head.ejs") %>
    <link rel="stylesheet" href="/public/css/style.css" />
    <script src="/public/js/index.js" defer></script>
    <style>
      .container {
        display: flex;
        width: 100%;
        height: 100vh; /* 화면 전체 높이 */
      }
      aside.rotation-chams-contatiner {
        flex: 0 0 10%; /* 가로 10% */
        background-color: #f1f1f1; /* 예시 배경 색 */
      }
      main {
        flex: 0 0 70%; /* 가로 70% */
        background-color: #fff; /* 예시 배경 색 */
      }
      aside.chat-contatiner {
        flex: 0 0 20%; /* 가로 20% */
        background-color: #f9f9f9; /* 예시 배경 색 */
      }
    </style>
  </head>

  <body>
    <aside class="rotation-chams-contatiner"></aside>
    <main class="main-container">
      <h1><%= title %></h1>
      <label
        >닉네임 태그
        <select id="tag">
          <option value="KR1">한국</option>
        </select>
      </label>
      <label> <input type="text" id="nickname" required /> </label>
      <button onclick="searchHandler()">검색</button>
    </main>
    <aside class="chat-contatiner"><%- include("./include/chat.ejs") %></aside>
  </body>
</html>
